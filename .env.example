# Optional. Without it FoodFlow runs on its local rules engine and the whole
# demo still works end to end — discovery, cart, authorisation, collection.
# With it, the agent's understanding and phrasing come from the model instead.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
